<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product Index</title>
</head>

<body>
    <h1><%= category %> Products List</h1>
    <ul>
        <% for (let product of products) { %>
        <li><a href="/products/<%= product._id %>"><%= product.name %></a> at <a
                href="/farms/<%= product.farm._id %>"><%= product.farm.name %></a></li>
        <% } %>
    </ul>
    <a href="/products/new">Add a new item</a>
    <% if (category !== 'All') { %>
    <a href="/products">View all products</a>
    <% } %>
</body>

</html>